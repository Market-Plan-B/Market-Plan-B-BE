# interinferencer.py

# === 라이브러리 ===
import json
from typing import Callable, Dict, Any

from langchain_core.messages import SystemMessage

from app.ai.state import AgentState


# === 공통 변수 정의 ===
INTERINFERENCER_SYSTEM_PROMPT: str = """
너는 SK 에너지의 Brent Oil 분석 에이전트에서
"목적 추론(Intent Inference)"만 담당하는 모듈이다.

너의 역할:
- 입력 텍스트를 보고,
- 유저가 실제로 알고 싶어 하는 목적을 아래 5가지 유형 중 하나로 분류하는 것이다.

[입력 형식]

아래 input_text 블록에 하나의 텍스트가 주어진다.

- 첫 턴(대화 히스토리가 비어 있을 때)에는:
  - [오늘 뉴스]
  - [예측 모델 결과]
  - [데일리 리포트]
  세 블록이 하나의 텍스트로 합쳐진 형태가 들어온다.

  여기서:
  - [오늘 뉴스]는 브렌트 관련 주요 비정형 뉴스 요약이다.
  - [예측 모델 결과]는 JSON 문자열일 수 있으며, 보통 다음 정보를 포함한다.
    - prediction: 오늘 종가, 예측 수익률, 예측 다음 종가 등
    - xai: 예측에 기여한 주요 요인(클러스터, 지표, 이벤트 등)의 중요도·방향성
  - [데일리 리포트]는 HTML 또는 텍스트 형식의 일간 보고서 요약이다.

  → 이때는 “오늘 시장 상황, 모델 예측(+XAI), 데일리 리포트 내용을 모두 고려했을 때,
    유저가 어떤 목적의 질문을 할 가능성이 높은지”를 추론한다.
  → 특히 모델의 예측 방향(상승/하락)과 XAI 상의 주요 드라이버(어떤 뉴스/지표가 가격을 밀었다고 보는지)를 참고하여,
    유저가 가장 먼저 물어볼 법한 목적 유형을 선택하라.

- 이후 턴(대화 히스토리가 존재할 때)에는:
  - [대화 히스토리]
  - [현재 유저 질문]
  두 블록이 하나의 텍스트로 합쳐진 형태가 들어온다.
  → 이때는 현재 질문 자체의 목적 유형을 분류하되,
    필요할 경우 직전 대화 흐름(히스토리)을 참고하여 맥락을 보완한다.

input_text:
\"\"\"{input_text}\"\"\"


아래 5가지 유형 정의를 기준으로, 현재 입력의 목적을 1개만 선택하라.

[5가지 목적 유형 정의 및 선택 기준]

아래 5가지 유형 중에서 하나만 선택해야 한다.
각 유형은 "질문의 초점이 어디에 있느냐"로 구분한다.

--------------------------------
1. 지표 해석형 (코드: 1)

[선택 기준]
- 질문의 초점이 **특정 지표/데이터 자체**에 맞추어져 있을 때 선택한다.
- 유저가 궁금해하는 것은:
  - "이 지표 숫자가 왜 이렇게 나왔는지"
  - "이 값이 보통 대비 어떤 의미인지"
  - "이 급증/급감이 노이즈인지, 의미 있는 신호인지"
- **유가 가격 방향 자체**보다는, 그 **배경이 되는 지표의 해석**이 중심일 때.

[자주 나오는 지표 예시]
- 수요(휘발유 수요, 제품 수요)
- 재고(EIA 재고, 상업 재고, 원유/제품 재고)
- 포지션(COT 순매수/순매도)
- 정제마진, 스프레드(crack spread), 수출입 물량 등

[질문 예시]
- “이번 주 휘발유 수요가 갑자기 튀었는데 왜 그래?”
- “COT 순매수 포지션이 급감했는데 이유가 뭐야?”
- “EIA 재고가 예상보다 많이 쌓였는데 이거 얼마나 큰 서프라이즈야?”

[헷갈릴 때 기준]
- 문장에 유가/가격이 등장하더라도,
  - **“이 지표가 뭔데?”**, **“이 숫자가 의미 있는 거야?”** 가 중심이면 → 지표 해석형(1)
  - **“그래서 유가가 왜 이렇게 움직였어?”** 가 중심이면 → 가격/리스크 설명형(2)

--------------------------------
2. 가격/리스크 설명형 (코드: 2)

[선택 기준]
- 질문의 초점이 **브렌트/유가 가격이 왜 이렇게 움직였는지**에 있을 때 선택한다.
- 유저가 궁금해하는 것은:
  - "오늘/이번 주 유가가 왜 올랐는지/떨어졌는지"
  - "이 흐름이 단기적으로 이어질지 말지"
  - "지금 리스크 구조가 상방이 더 큰지, 하방이 더 큰지"
- 지표는 **가격 움직임의 원인 재료**로만 사용될 뿐, 지표 자체를 해석하는 것이 1순위가 아닐 때.

[키워드 신호]
- “유가 / 브렌트 / 가격 / 급등 / 급락 / 변동성 / 빠졌어 / 올랐어 / 계속될까”

[질문 예시]
- “오늘 브렌트 왜 이렇게 떨어졌어?”
- “이번 주 상승이 계속될까?”
- “요즘 변동성이 너무 큰데, 뭐 때문에 이런 거야?”

[헷갈릴 때 기준]
- “오늘 재고 때문에 유가가 빠진 거야?” 같이 지표 + 유가가 같이 나와도,
  - 결론적으로 **유가 움직임**을 설명해달라는 느낌이면 → 가격/리스크 설명형(2)
  - 지표가 본론이고 유가는 부연이면 → 지표 해석형(1)

--------------------------------
3. 과거 유사사례 조회형 (코드: 3)

[선택 기준]
- 질문의 초점이 **“지금이 과거의 어느 상황과 비슷한지”**를 찾는 데 있을 때 선택한다.
- 유저가 궁금해하는 것은:
  - “지금처럼 ○○ 패턴이 과거에도 있었는지”
  - “그때 이후에 유가가 어떻게 움직였는지”
- 과거와의 **패턴 비교, 유사 구간 검색**이 핵심인 경우.

[키워드 신호]
- “예전에 / 과거에 / 역사적으로 / 코로나 때 / 금융위기 때 / 비슷한 적 / 비슷한 상황”

[질문 예시]
- “지금처럼 재고 늘면서 가격 버티는 구간 예전에 있었어?”
- “코로나 때랑 지금이 비슷해?”
- “이런 포지션 구도일 때 과거에는 이후에 어떻게 흘렀어?”

[헷갈릴 때 기준]
- 전망(4)은 **앞으로 어떻게 될지**를 묻는 것.
- 과거 유사사례(3)는 **과거에 이런 상황이 있었는지 + 그때 이후 흐름**을 찾는 것.
- 질문에 “과거 / 예전 / 그때 / 코로나 때 / 201x년” 같은 표현이 강하면 3번일 가능성이 높음.

--------------------------------
4. 전망형 (코드: 4)

[선택 기준]
- 질문의 초점이 **향후 일정 기간에 대한 방향성과 리스크**에 있을 때 선택한다.
- 유저가 궁금해하는 것은:
  - “다음 1주/1개월 동안 방향성이 어떻게 될지”
  - “상방/하방 중 어느 쪽 리스크가 더 큰지”
  - “어떤 이벤트/지표를 앞으로 봐야 하는지”
- 과거 이야기가 나오더라도, 결국 **미래 방향성**이 본론일 때.

[키워드 신호]
- “앞으로 / 향후 / 다음 주 / 다음 달 / 1주일 / 1개월 / 전망 / 방향 / 어디로 갈까 / 어떻게 봐야 해”

[질문 예시]
- “이번 EIA 발표 이후 1주일 방향 어떻게 봐야 해?”
- “다음 주까지 상방/하방 리스크 뭐가 더 커?”
- “단기적으로는 상방이야 하방이야?”

[헷갈릴 때 기준]
- 과거 유사사례(3)는 **“예전에 어땠냐”**가 중심.
- 전망형(4)은 **“그래서 앞으로 어떻게 될 것 같냐”**가 중심.
- 유저가 “앞으로, 다음 주, 다음 달, 향후”에 많이 집중하면 4번으로 본다.

--------------------------------
5. 액션·의사결정형 (코드: 5)

[선택 기준]
- 질문의 초점이 **“그래서 우리는 뭘 해야 하냐”**에 있을 때 선택한다.
- 유저가 궁금해하는 것은:
  - “재고를 늘릴지/줄일지”
  - “헤지 비율을 올릴지/낮출지”
  - “물류팀/경영진에게 어떤 메시지로 전달해야 할지”
- 즉, 시장 분석 자체보다는 **구체적인 행동 옵션/전략**이 본론일 때.

[키워드 신호]
- “줄일까 / 늘릴까 / 어떻게 해야 돼 / 전략 / 헤지 / 비율 / 조정 / 의사결정 / 말해야 해 / 보고해야 해 / 추천해줘”

[질문 예시]
- “다음 주 선적 앞두고 재고 줄이는 게 좋을까?”
- “우리 물류팀에 뭐라고 말해야 해?”
- “지금은 보수적으로 가져가는 게 나을까, 공격적으로 가져가도 될까?”

[헷갈릴 때 기준]
- 전망형(4)은 “방향/리스크”를 알고 싶어서 묻는 것.
- 액션형(5)은 “그래서 우리 회사/팀이 어떤 선택을 해야 하는지”가 핵심.
- 질문에 “해야 돼?”, “줄일까/늘릴까?”, “어떻게 가져가야 해?”, “뭐라고 말해야 해?” 같은 표현이 강하면 5번일 가능성이 높음.

[출력 형식]

반드시 하나의 JSON 객체만 출력하라.
JSON 이외의 텍스트, 코드블록, 주석을 추가하지 마라.

- "goal" 필드에는 1~5 중 하나의 정수 코드를 문자열로 넣는다. (예: "1", "2", ...)
- "reason" 필드에는, 현재 입력 상황에서 왜 그 유형이 적절한지 한 줄로 설명한다.

출력 예시:

{
  "goal": "4",
  "reason": "오늘과 같이 이벤트가 많은 구간에서는 향후 1주~1개월 방향성과 상·하방 리스크를 정리하는 전망형 목적이 우선순위가 높다"
}

지금 입력에 대해 위 JSON 형식으로 실제 결과를 출력하라.
"""


# === 공통 함수 정의 ===
def _build_content_for_inference(state: AgentState) -> str:
    """
    첫 턴에는 뉴스/모델 요약(JSON 문자열)/데일리 리포트를,
    이후에는 chat_history + user_input을 사용해 목적 추론 입력 텍스트를 만든다.
    """
    user_input = state.get("user_input", "") or ""
    chat_history = state.get("chat_history", []) or []

    # 1) 완전 첫 턴: 히스토리도 없고 유저 질문도 없는 경우
    if not chat_history and not user_input:
        news = state.get("daily_news", "") or ""
        model = state.get("model_result", "") or ""
        report = state.get("daily_report", "") or ""
        return (
            f"[오늘 뉴스]\n{news}\n\n"
            f"[예측 모델 결과]\n{model}\n\n"
            f"[데일리 리포트]\n{report}\n"
        )

    # 2) 첫 턴이지만, 유저 질문이 바로 들어온 경우: 질문만 사용
    if not chat_history:
        return user_input

    # 3) 이후 턴: 최근 히스토리 + 현재 질문을 함께 사용
    history_texts = []
    # 최근 N개만 사용 (필요하면 조정 가능)
    recent_msgs = chat_history[-5:]

    for msg in recent_msgs:
        # langchain BaseMessage 기준: .type 또는 .role / .content
        role = getattr(msg, "type", getattr(msg, "role", "user"))
        content = getattr(msg, "content", "")
        history_texts.append(f"[{role}] {content}")

    history_block = "\n".join(history_texts)

    return (
        f"[대화 히스토리]\n{history_block}\n\n"
        f"[현재 유저 질문]\n{user_input}"
    )


# === 실행 함수 정의 ===
def build_interinferencer_node(llm_json) -> Callable[[AgentState], Dict[str, Any]]:
    """
    목적 추론 노드를 생성한다.
    LLM 응답은 {"goal": "...", "reason": "..."} 형태의 JSON이라고 가정한다.
    """

    def node(state: AgentState) -> Dict[str, Any]:
        input_text = _build_content_for_inference(state)

        prompt = INTERINFERENCER_SYSTEM_PROMPT.replace("{input_text}", input_text)

        messages = [SystemMessage(content=prompt)]
        resp = llm_json.invoke(messages)

        goal = ""
        reason = ""

        try:
            data = json.loads(resp.content)
            goal = data.get("goal", "") or ""
            reason = data.get("reason", "") or ""
        except Exception:
            # 실패 시 기본값 (가격/리스크 설명형)으로 설정
            goal = "2"
            reason = "응답을 파싱하지 못해 기본적으로 가격/리스크 설명형으로 분류했습니다."

        return {
            "goal": goal,
            "goal_reason": reason,
        }

    return node
